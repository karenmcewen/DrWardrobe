@model IEnumerable<DrWardrobe.Models.Occasion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<img src="~/Images/gallifreyan writing.png" />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OccasionType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OccasionType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OccasionID }) |
          
            @Html.ActionLink("Delete", "Delete", new { id=item.OccasionID })
        </td>
    </tr>
}

</table>
